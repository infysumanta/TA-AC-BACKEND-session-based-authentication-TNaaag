<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <nav class="bg-primary py-4 px-8 flex justify-between items-center">
      <a href="/" class="text-white text-3xl font-bold">Home</a>
      <div class="w-64 flex justify-between items-center">
        <a
          href="/articles"
          class="bg-blue-900 p-2 rounded text-white font-bold hover:bg-blue-600 text-xl"
          >Articles List</a
        >
        <a
          href="/users/logout"
          class="bg-blue-900 p-2 rounded text-white font-bold hover:bg-blue-600 text-xl"
          >Logout</a
        >
      </div>
    </nav>

    <div class="flex justify-center items-center h-90">
      <form
        action="/articles/<%= article.slug %>"
        method="POST"
        class="border border-gray-300 flex flex-col items-center justify-center w-1/3 py-8 px-4 rounded-lg"
      >
        <input
          type="text"
          name="title"
          placeholder="Enter the title"
          class="block py-3 px-4 bg-gray-200 my-2 w-custom rounded-lg border border-gray-300"
          value="<%= article.title %>"
        />
        <textarea
          type="text"
          name="description"
          placeholder="Enter the description"
          rows="5"
          class="block bg-gray-200 my-2 w-custom rounded-lg p-2 border border-gray-300"
          value="<%= article.description %>"
        >
<%= article.description %></textarea
        >
        <input
          type="text"
          name="author"
          placeholder="Enter the author name"
          class="block py-3 px-4 bg-gray-200 my-2 w-custom rounded-lg border border-gray-300"
          value="<%= article.author %>"
        />
        <input
          type="submit"
          value="Update Article"
          class="w-custom rounded-lg mt-4 block py-3 px-4 bg-blue-700 text-white font-bold cursor-pointer hover:bg-blue-400"
        />
      </form>
    </div>
  </body>
</html>
