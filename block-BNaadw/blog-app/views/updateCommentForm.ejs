<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <nav class="bg-primary py-4 px-8 flex justify-between items-center">
      <a href="/" class="text-white text-3xl font-bold">Home</a>
      <div class="w-64 flex justify-between items-center">
        <a
          href="/articles"
          class="bg-blue-900 p-2 rounded text-white font-bold hover:bg-blue-600 text-xl"
          >Articles List</a
        >
        <a
          href="/users/logout"
          class="bg-blue-900 p-2 rounded text-white font-bold hover:bg-blue-600 text-xl"
          >Logout</a
        >
      </div>
    </nav>

    <div class="w-1/3 mx-auto h-90 flex justify-center items-center">
      <form
        action="/comments/<%= comment.id %>"
        method="POST"
        class="flex flex-col items-center justify-center px-2 py-6 rounded-lg border border-gray-400"
      >
        <textarea
          name="title"
          cols="40"
          rows="6"
          class="mb-4 p-2 rounded-lg w-custom border border-gray-400 bg-gray-200"
        >
<%= comment.title %></textarea
        >
        <input
          type="text"
          name="author"
          placeholder="Enter the Author Name"
          class="p-2 mb-4 rounded-lg border border-gray-400 bg-gray-200 w-custom"
          value="<%= comment.author %>"
        />
        <input
          type="submit"
          value="Update Comment"
          class="my-1 bg-blue-500 text-white font-bold w-custom p-2 rounded-lg cursor-pointer hover:bg-blue-400"
        />
      </form>
    </div>
  </body>
</html>
